@page
@model EmailModel
@{
    ViewData["Title"] = "ASP.NET Core and Microsoft Graph";
}

<div>
    <h1>Email</h1>
    @if (User.Identity.IsAuthenticated)
    {
        if (Model.Messages.Count > 0) {
            @foreach(var message in Model.Messages) {
                <div class="card" style="width: 50rem;">
                    <div class="card-body">
                        <h5 class="card-title">@message.Subject</h5>
                        <h6 class="card-subtitle mb-2 text-muted">@message.ReceivedDateTime.GetValueOrDefault().UtcDateTime</h6>
                        <p class="card-text">@message.BodyPreview</p>
                    </div>
                </div>
                <br />
            }
            @if (Model.Skip != 0) {
                <a asp-page="/Email" asp-route-skip="@Model.Skip" class="btn btn-primary">Next Page</a>
            }
        }
        else {
            <div>No Messages found</div>
        }
    }
    else
    {
        <a class="btn btn-primary btn-large" asp-area="MicrosoftIdentity" asp-controller="Account" asp-action="SignIn">Click here to sign in</a>
    }
</div>